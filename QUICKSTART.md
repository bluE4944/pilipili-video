# 快速开始指南

## 一、安装依赖

```bash
npm install
```

## 二、启动开发服务器

```bash
npm run serve
```

访问 `http://localhost:8080`

## 三、基本使用流程

### 1. 添加视频

1. 点击顶部导航栏的"视频"菜单
2. 点击"添加视频"按钮
3. 在文件选择器中选择视频文件（支持多选）
4. 系统会自动识别并整合为视频合集

### 2. 播放视频

1. 在视频列表页点击视频卡片
2. 进入视频详情页，自动加载上次播放位置
3. 点击分集列表切换不同集数
4. 播放完成后自动切换到下一集

### 3. 主题切换

点击顶部导航栏右侧的主题切换按钮（太阳/月亮图标）

### 4. 账号功能

- 点击用户按钮可登录/注册
- 或使用"游客模式"（无需注册）

## 四、功能说明

### ✅ 已实现功能

- ✅ 视频文件添加（通过文件选择器）
- ✅ 视频合集自动识别和整合
- ✅ 视频播放（支持进度记忆）
- ✅ 自动下一集
- ✅ 主题切换（浅色/深色/跟随系统）
- ✅ 账号系统（注册/登录/游客模式）
- ✅ 评论功能（本地存储）

### ⚠️ 已知限制

1. **文件系统访问**: 由于浏览器安全限制，无法直接访问本地文件系统，需要通过文件选择器添加视频
2. **文件持久化**: 刷新页面后需要重新选择视频文件（元数据会保留）
3. **视频格式**: 推荐使用 MP4 格式

### 🔮 后续扩展

- Electron 桌面应用（支持文件系统访问）
- 后端 API 集成
- 弹幕功能
- 视频转码服务

## 五、常见问题

### Q: 如何实现真正的文件夹扫描？

A: 当前版本由于浏览器限制，需要通过文件选择器添加视频。如需实现文件夹扫描，可以：
1. 使用 Electron 包装为桌面应用
2. 配置后端 API，通过服务器扫描文件系统
3. 使用 File System Access API（需要用户授权，Chrome/Edge 支持）

### Q: 播放记录会丢失吗？

A: 播放记录存储在 localStorage，清除浏览器数据会导致丢失。建议定期导出数据或使用后端存储。

### Q: 如何部署到局域网？

A: 
1. 开发环境：修改 `vue.config.js` 中的 `host: '0.0.0.0'`（已配置）
2. 生产环境：构建后使用静态文件服务器部署

## 六、技术栈

- Vue 3 + TypeScript
- Naive UI
- Video.js
- Pinia
- Vue Router 4

详细技术选型说明请查看 [docs/技术选型说明.md](./docs/技术选型说明.md)
