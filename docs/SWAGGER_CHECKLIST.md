# Swagger API 检查清单

使用此清单来系统地检查和调整 API 端点。

## 准备工作

- [ ] 后端服务已启动
- [ ] 可以访问 `http://localhost:8316/swagger-ui.html`
- [ ] 已打开 `src/api/` 目录下的所有 API 文件

## 视频相关 API 检查

### 视频合集

- [ ] **获取所有合集**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/collections`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取合集详情**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/collections/:id`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

### 视频文件

- [ ] **获取所有视频**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/videos`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取视频详情**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/videos/:id`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **上传视频**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/upload`
  - 请求字段名: `file / videoFile / video / 其他: _____`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **批量上传视频**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/upload/batch`
  - 请求字段名: `files / videoFiles / 其他: _____`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取播放URL**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/videos/:id/play-url`
  - 响应格式: `{ url: string } / { data: { url: string } } / 其他: _____`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **扫描文件夹**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/scan`
  - 请求体格式: `{ folderPath: string } / 其他: _____`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

## 播放记录 API 检查

- [ ] **保存播放记录**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/play-records`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取播放记录**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/video/play-records/:videoId`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

## 文件夹配置 API 检查

- [ ] **获取所有配置**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/folder/configs`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **添加配置**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/folder/configs`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **更新配置**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/folder/configs/:id`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **删除配置**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/folder/configs/:id`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

## 评论 API 检查

- [ ] **获取视频评论**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/comment/video/:videoId`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取合集评论**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/comment/collection/:collectionId`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **添加评论**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/comment`
  - 请求体字段: `_________________`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

## 用户 API 检查

- [ ] **登录**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/user/login`
  - 请求体字段: `username, password / 其他: _____`
  - 响应格式: `{ token: string, user: User } / 其他: _____`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **注册**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/user/register`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

- [ ] **获取当前用户**
  - Swagger 路径: `_________________`
  - 方法: `GET / POST / PUT / DELETE`
  - 当前代码路径: `/api/user/me`
  - 需要修改: `是 / 否`
  - 修改内容: `_________________`

## 通用检查项

- [ ] **API 基础路径**
  - 是否有统一的前缀？`是 / 否`
  - 前缀是什么？`_________________`
  - 需要在 `src/utils/api.ts` 中修改 baseURL 吗？`是 / 否`

- [ ] **认证方式**
  - 使用 Bearer Token？`是 / 否`
  - Token 在请求头中的字段名：`Authorization / X-Auth-Token / 其他: _____`
  - Token 格式：`Bearer {token} / {token} / 其他: _____`

- [ ] **响应格式**
  - 统一响应格式？`是 / 否`
  - 格式示例：`{ code: 200, data: {...}, message: '...' } / 其他: _____`
  - 成功状态码：`200 / 其他: _____`
  - 需要在 `src/utils/api.ts` 中修改响应拦截器吗？`是 / 否`

- [ ] **错误处理**
  - 错误响应格式：`_________________`
  - HTTP 状态码含义：
    - 401: `未授权 / 其他: _____`
    - 403: `权限不足 / 其他: _____`
    - 404: `资源不存在 / 其他: _____`
    - 500: `服务器错误 / 其他: _____`

## 完成检查后

1. 根据检查结果修改 `src/api/` 目录下的文件
2. 如有需要，修改 `src/utils/api.ts` 中的拦截器
3. 测试各个功能模块
4. 查看浏览器控制台的网络请求，确认请求格式正确
